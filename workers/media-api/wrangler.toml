name = "tiko-media-api-dev"
main = "src/index.ts"
compatibility_date = "2026-05-27"
account_id = "8cef251b5fdcf6c6f63db98b7aa49f9a"
workers_dev = true

# Unified media-api worker combining:
#   media-upload  → R2 uploads + OpenAI Vision analysis
#   media-cache   → D1 media listing (KV caching skipped for now)
#   assets-upload → R2 uploads + D1 asset tracking

# ── D1 Databases ──────────────────────────────────────────────

[[d1_databases]]
binding = "MEDIA_DB"
database_name = "tiko-media"
database_id = "cc80e3db-b6c4-4446-aab5-fcb67b950a0e"

[[d1_databases]]
binding = "ASSETS_DB"
database_name = "tiko-assets-db-dev"
database_id = "35b4e4f4-f9aa-447c-a7d7-f4d58a08420a"

# ── R2 Buckets ────────────────────────────────────────────────

[[r2_buckets]]
binding = "MEDIA_BUCKET"
bucket_name = "media"

[[r2_buckets]]
binding = "ASSETS_BUCKET"
bucket_name = "tiko-assets"

[[r2_buckets]]
binding = "USER_MEDIA_BUCKET"
bucket_name = "user-media"

# ── Secrets (set via `wrangler secret put`) ────────────────────
# OPENAI_API_KEY
# IDENTITY_BASE_URL — optional, defaults to https://api.tikoapi.org/v1

[[d1_databases]]
binding = "AUTH_DB"
database_name = "tiko-identity"
database_id = "0b5e035d-6420-451e-8e54-0af5829cc95c"
