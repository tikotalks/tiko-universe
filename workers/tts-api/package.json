{
  "name": "@tiko-worker/tts-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --skipLibCheck src/index.ts",
    "deploy:dry-run": "npx -y -p node@22 -p wrangler wrangler deploy --dry-run --outdir dist"
  }
}
