# iOS Apps

Native SwiftUI apps live here. Use XcodeGen project files and do not commit generated `.xcodeproj` files.

Recommended first native proof: Yes No.

The iOS app must consume the same OpenAPI contract as web and Android.
