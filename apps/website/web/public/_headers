# TikoTalks is changing rapidly during launch. Revalidate every app shell
# and built asset so browsers don't keep stale JS/CSS that require hard refreshes.
/*
  Cache-Control: public, max-age=0, must-revalidate

/assets/*
  Cache-Control: public, max-age=0, must-revalidate
