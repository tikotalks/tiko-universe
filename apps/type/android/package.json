{
  "name": "@tiko-android/type",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:web": "npm --prefix ../../.. run build --workspace @tiko-universe/type-web",
    "sync": "npm run build:web && cap sync android",
    "build:android": "npm run sync && cd android && ./gradlew assembleDebug",
    "open": "cap open android"
  }
}
