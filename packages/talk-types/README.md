# @tiko/talk-types

Shared TypeScript contracts for the Tiko Talk sentence-building app.

This package is the source of truth for the web client, Sentence API Worker, and future native clients. It contains only API/domain types and small type guards; sentence intelligence stays in `workers/sentence-api`.
